# Card 01 - Contract Fingerprint

Owner: A0 Lead Integrator
Status: `READY`
Tasks: `V4-SYS-001..003`
Depends on: none

## Objective

Lock current source/worktree evidence, guest-local invariants and route tiers before any UI implementation.

## Writable Files

V4 plan/status docs only. Product source is read-only.

## Checklist

- record Git SHA when available plus dirty/untracked fingerprint
- confirm npm, Pages Router and `guest-local`
- list core, entry-hold, secondary-hold and API route tiers
- record behavior invariants for quiz/result/history/reconnect/PNG

## Acceptance And Evidence

Every current route has one tier; no implementation task can reinterpret cloud/auth/scoring scope. Save fingerprint under `output/ui-redesign-v4/<date>/program-contract/`.
