# Packet 00 - Program Contract

Owner: A0 Lead Integrator
Dependencies: none
Output: locked V4 scope, route tiers, shared-file table, evidence convention and approval gates

## Tasks

- `V4-SYS-001`: fingerprint current source, dirty worktree and relevant untracked V3/V4 files without staging or normalizing root history.
- `V4-SYS-002`: lock `guest-local`, scoring, persistence, reconnect and PNG export as behavior invariants.
- `V4-SYS-003`: lock six core route families and held-route tiers.
- `V4-SYS-004`: lock Navbar/locale ownership and three-primary-link IA.
- `V4-SYS-005`: lock visual thesis, anti-direction and card usage rules.
- `V4-SYS-006`: publish one-writer shared-file claims.
- `V4-SYS-007`: define evidence naming, viewport/state/locale metadata and freshness requirements.
- `V4-SYS-008`: define concept-reference versus production-asset boundary.
- `V4-SYS-009`: define approval gates and stop conditions.
- `V4-SYS-010`: sign Definition of Ready/Done for every execution card.

## Acceptance

- every route and shared file has one declared tier/owner
- no task implies auth/cloud/scoring activation
- V3 remains historical baseline and V4 evidence cannot reuse stale screenshots
- downstream agent can identify exact dependency and stop condition without guessing
