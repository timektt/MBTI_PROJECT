# Packet 01 - Audit And Pruning

Owner: A1 UX Audit Agent
Dependencies: `V4-SYS-001..010`
Output: screenshot baseline, route/state inventory, overlap diagnosis, prune decisions and content hierarchy

## Tasks

- `V4-AUD-001`: capture current core routes at 320, 390, 768, 1024 and 1440.
- `V4-AUD-002`: create deterministic quiz/result/dashboard fixture states from current guest runtime.
- `V4-AUD-003`: audit Navbar open/closed, TH/EN, keyboard and 200% zoom states.
- `V4-AUD-004`: map every visible overlap, clipping, horizontal overflow and stacking-context cause.
- `V4-AUD-005`: run squint-test hierarchy assessment per core route.
- `V4-AUD-006`: inventory cards, nested containers, repeated CTA groups and repeated metadata.
- `V4-AUD-007`: inventory page-local locale/status controls and redundant route headers.
- `V4-AUD-008`: classify every visible surface as KEEP, REBUILD, DEMOTE, CONSOLIDATE, REMOVE or HOLD.
- `V4-AUD-009`: map active import/use sites before any component/CSS removal.
- `V4-AUD-010`: define one primary outcome and reading order for each core route.
- `V4-AUD-011`: publish route-specific responsive failure table and severity.
- `V4-AUD-012`: produce the first Home approval packet with before screenshots and exact proposed edits.

## Acceptance

- findings cite screenshot, DOM/runtime or file evidence
- every remove decision lists replacement/access path
- no implementation file changed in this packet
- Home packet is specific enough for user approval and A2/A3 briefs
